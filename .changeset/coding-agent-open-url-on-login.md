---
"@jay-ai/coding-agent": minor
---

Automatically open the OAuth authorization URL in the user's default browser during `/login` (and at first-run login). The URL is still printed to the terminal as a fallback.
